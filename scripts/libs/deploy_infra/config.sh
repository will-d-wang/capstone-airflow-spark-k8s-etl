ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"

NAMESPACE="${NAMESPACE:-ai-core-pipeline}"
PROFILE="${MINIKUBE_PROFILE:-ai-core-etl}"
TERRAFORM_DIR="${TERRAFORM_DIR:-${ROOT_DIR}/infra/terraform}"
RELEASE_NAME="${RELEASE_NAME:-airflow}"
HELM_TIMEOUT="${HELM_TIMEOUT:-20m}"
HELM_ATOMIC="${HELM_ATOMIC:-true}"
AIRFLOW_IMAGE="${AIRFLOW_IMAGE:-local/airflow-custom:dev}"
RUN_SEED_DATA_JOB="${RUN_SEED_DATA_JOB:-true}"
CONFIGURE_LOCAL_ACCESS="${CONFIGURE_LOCAL_ACCESS:-true}"
SKIP_BOOTSTRAP="${SKIP_BOOTSTRAP:-false}"
HOSTS_FILE="${HOSTS_FILE:-/etc/hosts}"
MINIO_HEALTH_URL="${MINIO_HEALTH_URL:-http://minio-api.local/minio/health/live}"
MINIO_HEALTH_RETRIES="${MINIO_HEALTH_RETRIES:-24}"
MINIO_HEALTH_INTERVAL_SECONDS="${MINIO_HEALTH_INTERVAL_SECONDS:-5}"
S3_BUCKET="${S3_BUCKET:-lake}"

POSTGRES_USER="${POSTGRES_USER:-airflow}"
POSTGRES_PASSWORD="${POSTGRES_PASSWORD:-airflow}"
POSTGRES_DB="${POSTGRES_DB:-airflow}"
MINIO_ROOT_USER="${MINIO_ROOT_USER:-minioadmin}"
MINIO_ROOT_PASSWORD="${MINIO_ROOT_PASSWORD:-minioadmin123}"
AIRFLOW_ADMIN_USERNAME="${AIRFLOW_ADMIN_USERNAME:-admin}"
AIRFLOW_ADMIN_PASSWORD="${AIRFLOW_ADMIN_PASSWORD:-password123}"
AIRFLOW_ADMIN_EMAIL="${AIRFLOW_ADMIN_EMAIL:-admin@example.com}"
AIRFLOW_ADMIN_FIRST_NAME="${AIRFLOW_ADMIN_FIRST_NAME:-Admin}"
AIRFLOW_ADMIN_LAST_NAME="${AIRFLOW_ADMIN_LAST_NAME:-User}"
